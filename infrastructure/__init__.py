"""Concrete adapters for external runtime capabilities.

Sub-packages:
- ``persistence/`` — storage adapters (ConfigRepository, BackupStore, HistoryStore)
- ``process/`` — process management (subprocess, shell execution)
- ``windows/`` — Windows-specific infrastructure

Top-level modules:
- ``system_clock.py`` — Clock port adapter
"""

"""Pure domain entities and value objects.

Key modules:
- ``models.py`` — Core domain models (ShortcutItem, Folder, AppSettings, AppData)
- ``clipboard.py`` — Clipboard abstraction

Error base class: ``application.errors.DomainError`` — use for domain rule violations.
"""

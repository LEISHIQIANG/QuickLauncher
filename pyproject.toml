[tool.black]
line-length = 120
target-version = ["py312"]

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
    ".git",
    ".pytest_cache",
    "dist",
    "build",
    "temp_icons",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
